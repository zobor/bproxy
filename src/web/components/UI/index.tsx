import {
  Button,
  Card,
  Col,
  Form,
  Input,
  message,
  Modal,
  Radio,
  Row,
  Select,
  Spin,
  Switch,
  Tag,
  Tooltip,
  Checkbox,
} from 'antd';

message.config({
  top: 100,
  duration: 1.5,
  maxCount: 2,
});

export { Checkbox, Tag, Spin, Card, Col, Row, Tooltip, Button, message, Switch, Form, Input, Select, Radio, Modal };
